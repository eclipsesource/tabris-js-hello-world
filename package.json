{
  "name": "hello-world",
  "version": "0.5.1",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "tabris serve -a -w",
    "start:gitpod": "tabris serve -a -w --no-intro --port 8080 --external $(gp url 8080):443"
  },
  "dependencies": {
    "tabris": "nightly"
  },
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "~3.5.3"
  }
}
