{
  "name": "hello-world",
  "version": "0.5.5",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris": "3.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "tslint": "^5.11.0",
    "typescript": "^3.2.4"
  }
}
