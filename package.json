{
  "name": "hello-world",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris": "2.6.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.2",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  }
}
