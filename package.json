{
  "name": "hello-world",
  "version": "0.4.0",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris": "nightly"
  },
  "devDependencies": {
    "npm-run-all": "^4.0.2",
    "tslint": "^5.4.2",
    "typescript": "^2.3.4"
  }
}
