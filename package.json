{
  "name": "hello-world",
  "version": "0.5.7",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris": "3.3.0"
  },
  "devDependencies": {
    "tslint": "^5.20.0",
    "typescript": "3.3.x"
  }
}
